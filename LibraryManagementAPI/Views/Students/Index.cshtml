@model IEnumerable<LibraryManagementAPI.Models.Student>

<div class="student-list-container">
    <h2 class="text-center">Students List</h2>

    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>Student Name</th>
                <th>Date of Birth</th>
                <th>Email</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var student in Model)
            {
                <tr>
                    <td>@student.StudentName</td>
                    <td>@student.DOB.ToShortDateString()</td>
                    <td>@student.Email</td>
                    <td>
                        <a class="btn btn-sm btn-warning me-2" asp-action="Edit" asp-route-id="@student.StudentID">Edit</a>
                        <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@student.StudentID">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <div class="text-end mt-3">
        <a class="btn btn-primary" asp-action="Create">Add New Student</a>
    </div>
</div>

<style>
    .student-list-container {
        max-width: 900px;
        margin: 20px auto;
        padding: 20px;
        background-color: #f9f9f9;
        border: 1px solid #ddd;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
    h2 {
        color: #007bff;
        margin-bottom: 20px;
    }
    .table {
        border: 1px solid #ddd;
        border-radius: 5px;
        overflow: hidden;
    }
    .table th, .table td {
        vertical-align: middle;
        text-align: center;
    }
    .table thead th {
        background-color: #343a40;
        color: #fff;
    }
    .btn {
        font-size: 0.9rem;
    }
    .btn-warning {
        background-color: #ffc107;
        border-color: #ffc107;
        color: #212529;
    }
    .btn-danger {
        background-color: #dc3545;
        border-color: #dc3545;
    }
    .btn-primary {
        background-color: #007bff;
        border-color: #007bff;
    }
</style>
